version: v1.0
name: CI

agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2204

blocks:
  - name: Run tests
    task:
      jobs:
        - name: Run Docker
          commands:
            - checkout
            - docker run --rm aitesla/ai:ai
