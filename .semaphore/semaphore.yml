version: v1.0
name: AI Pipeline

agent:
  machine:
    type: e1-standard-2

blocks:
  - name: Run AI
    task:
      jobs:
        - name: start
          commands:
            - docker run --rm aitesla/ai:ai
