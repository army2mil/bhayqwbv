version: v1.0
name: "\U0001F680 Go Project CI/CD Pipeline"
agent:
  machine:
    type: r1-standard-8
    os_image: ubuntu2404
blocks:
  - name: Test Docker Runtime
    task:
      jobs:
        - name: sanity-check
          commands:
            - docker version
            - 'docker run --rm alpine:3.20 echo "container ok (alpine works)"'
            - 'docker pull aitesla/ai:ai || true'
            - echo "skip broken image execution"
